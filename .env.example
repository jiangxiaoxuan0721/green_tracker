# =============================================================================
# 环境配置示例文件 - Green Tracker 项目
# =============================================================================
# 使用说明：
# 1. 复制此文件为 .env 并根据实际情况填写配置
# 2. 请勿将包含敏感信息的 .env 文件提交到版本控制系统

# =============================================================================
# 应用基础配置
# =============================================================================

# 应用环境 (development, production)
NODE_ENV=development

# =============================================================================
# 前端配置 (React)
# =============================================================================

# 前端服务器端口
PORT=3010

# 前端API请求基础URL
# 注意：这里使用API_HOST和API_PORT的组合，避免硬编码
VITE_API_BASE_URL=http://localhost:6130

# 开发环境默认登录凭据（仅用于开发测试）
VITE_DEFAULT_USERNAME=your_default_username
VITE_DEFAULT_PASSWORD=your_default_password

# 允许的主机列表（逗号分隔）
# 示例：localhost,127.0.0.1,yourdomain.com,.yourdomain.com
VITE_ALLOWED_HOSTS=your_allowed_hosts_comma_separated

# =============================================================================
# 后端API配置
# =============================================================================

# API服务器主机配置
API_HOST=0.0.0.0
API_PORT=6130

# JWT认证配置
# 注意：在生产环境中请使用强随机密钥（至少32字符）
SECRET_KEY=please_generate_a_strong_random_secret_key_here
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=30

# =============================================================================
# 数据库配置 (PostgreSQL)
# =============================================================================

DB_HOST=your_db_host
DB_PORT=5432
DB_NAME=your_user_database_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# 元数据库名称（固定，用于存储用户、数据库等元数据）
META_DB_NAME=green_tracker_meta

# 超级用户配置（用于创建数据库和PostGIS扩展）
DB_SUPERUSER=postgres
DB_SUPERPASSWORD=your_superuser_password

# 是否使用Unix Socket连接（Peer认证，不需要密码）
# true: 使用Unix Socket，不需要密码
# false: 使用TCP连接，需要密码（推荐）
USE_UNIX_SOCKET=false

# 数据库连接池配置（可选）
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20

# =============================================================================
# 对象存储配置 (MinIO)
# =============================================================================

MINIO_ENDPOINT=localhost
MINIO_PORT=6130
MINIO_ACCESS_KEY=your_minio_access_key
MINIO_SECRET_KEY=your_minio_secret_key
MINIO_BUCKET_NAME=green-tracker-assets

# MinIO安全配置（生产环境建议启用）
MINIO_SECURE=false

# =============================================================================
# 日志配置
# =============================================================================

# 日志级别 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# 日志文件路径（可选，默认为控制台输出）
LOG_FILE_PATH=

# =============================================================================
# CORS配置
# =============================================================================

# 允许的源（逗号分隔）
CORS_ORIGINS=your_allowed_origins_comma_separated

# 允许的方法（逗号分隔）
CORS_METHODS=GET,POST,PUT,DELETE,OPTIONS

# 允许的头部（逗号分隔）
CORS_HEADERS=Content-Type,Authorization

# =============================================================================
# 文件上传配置
# =============================================================================

# 最大文件上传大小（字节）
MAX_UPLOAD_SIZE=10485760

# 允许的文件类型（逗号分隔）
ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,pdf,doc,docx